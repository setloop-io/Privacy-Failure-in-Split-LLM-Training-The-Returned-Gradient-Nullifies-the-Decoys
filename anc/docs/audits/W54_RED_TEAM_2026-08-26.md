# W5.4 sealed red team: independent re-derivation of the E1 campaign findings

**Date:** 2026-08-26. **Campaign item:** W5.4.
**Verdict: the campaign's findings reproduce under independent re-derivation.**

A sealed, independently-implemented red team re-derived the campaign's key
findings from the raw `.pt` bundles using its own scorer (`/tmp/redteam/rescore.py`),
without calling `bin/paired_advantage.py`, `bin/leakage_metrics.py`, `attacker/`,
or any packaged pipeline, and without touching any tuning knob. Nothing was
committed to git by the red team; the repo is untouched.

## Result

| cell | red team (pp) | committed (pp) | delta | pass @0.01pp |
| --- | --- | --- | --- | --- |
| s42 (W1.7) | +1.1922 | +1.1923 | <0.000001 | PASS |
| s43 | +0.6893 | +0.6893 | <0.000001 | PASS |
| s44 | +0.6929 | +0.6929 | <0.000001 | PASS |
| s45 | +0.8435 | +0.8435 | <0.000001 | PASS |
| s46 | +0.9361 | +0.9361 | <0.000001 | PASS |
| s47 | +0.7705 | +0.7705 | <0.000001 | PASS |
| s48 (W5.7) | +0.6456 | +0.6456 | <0.000001 | PASS |
| s49 | +1.5008 | +1.5008 | <0.000001 | PASS |
| s50 | +0.9929 | +0.9929 | <0.000001 | PASS |

Matched shuffled nulls identical to committed in all nine cells; all 162
arm-level values match to ≤ 1e-6 pp; control class, cluster counts, correct
counts, and best-arm picks all match.

## Structural finding (support_leak)

Re-derived from the raw grad-channel captures on **all nine** seeds: a chaff
row's gradient is identically zero, so the zero-row pattern exactly matches the
real/chaff partition in **4096/4096 frames with row agreement 1.0** in every
one -- not just the s44 cell cited. Magnitude cross-check matches the committed
`magnitudes` block to 6 significant figures.

## Adversarial notes (the red team's own honesty)

1. **Scope boundary:** the re-derivation starts from the committed attacker
   *prediction tensors*; the attacker's own prediction generation is out of
   sealed scope (not re-implemented). The scoring chain from attacker outputs
   to published numbers is verified end-to-end.
2. **The null is a real matched null** (bit-identical wire, joint token
   reshuffle only) -- the ~0 pp nulls are not an artifact of easier data.
3. **Best-of-9 selection** is mildly upward-biased as a point estimate, but
   every one of the 9 arms is positive in every grad_real cell, so the finding
   does not hinge on the selection.
4. **CI95 not exactly reproducible** (resampling method/RNG is sealed); both
   bounds agree within ~0.02 pp and are far above 0 in all real cells.

## What this closes

The audit's acceptance criterion: the frozen artifacts handed to a sealed,
independently implemented red team reproduce the campaign's findings, without
retuning. The E1 gradient-channel claim survives adversarial re-derivation from
the raw bundles.
